import { useEffect, useState } from 'react';
import { BarChart, Bar, CartesianGrid, Legend, ResponsiveContainer, Tooltip, XAxis, YAxis, LineChart, Line } from 'recharts';
import http from '../api/http'; import StatCard from '../components/StatCard';
const formatCurrency = (v) => `Rp ${Number(v || 0).toLocaleString('id-ID')}`;
export default function DashboardPage() { const [data, setData] = useState(null); useEffect(() => { http.get('/dashboard').then((res) => setData(res.data)); }, []); if (!data) return <div>Loading dashboard...</div>; return <div className="space-y-6"><div><h1 className="text-3xl font-bold text-slate-900">Dashboard</h1><p className="mt-1 text-slate-500">Ringkasan pemasukan, pengeluaran, dan transaksi yang menunggu approval.</p></div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4"><StatCard title="Total Pemasukan" value={formatCurrency(data.summary.total_income)} /><StatCard title="Total Pengeluaran" value={formatCurrency(data.summary.total_expense)} /><StatCard title="Net Balance" value={formatCurrency(Number(data.summary.total_income) - Number(data.summary.total_expense))} /><StatCard title="Pending Approval Kas Besar" value={data.pendingBigCash} subtitle="Wajib 3 approver" /></div><div className="grid gap-6 xl:grid-cols-2"><div className="card p-5"><h3 className="text-lg font-semibold">Trend Bulanan</h3><div className="mt-4 h-80"><ResponsiveContainer width="100%" height="100%"><LineChart data={data.monthly}><CartesianGrid strokeDasharray="3 3" /><XAxis dataKey="month" /><YAxis /><Tooltip formatter={(v) => formatCurrency(v)} /><Legend /><Line type="monotone" dataKey="income" name="Pemasukan" /><Line type="monotone" dataKey="expense" name="Pengeluaran" /></LineChart></ResponsiveContainer></div></div><div className="card p-5"><h3 className="text-lg font-semibold">Kas Kecil vs Kas Besar</h3><div className="mt-4 h-80"><ResponsiveContainer width="100%" height="100%"><BarChart data={data.byCashType}><CartesianGrid strokeDasharray="3 3" /><XAxis dataKey="cash_type" /><YAxis /><Tooltip formatter={(v) => formatCurrency(v)} /><Legend /><Bar dataKey="income" name="Pemasukan" /><Bar dataKey="expense" name="Pengeluaran" /></BarChart></ResponsiveContainer></div></div></div></div>; }
