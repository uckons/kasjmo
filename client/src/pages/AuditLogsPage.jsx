import { useEffect, useState } from 'react';
import http from '../api/http';
export default function AuditLogsPage() { const [rows, setRows] = useState([]); useEffect(() => { http.get('/audit-logs').then((res) => setRows(res.data)); }, []); return <div className="space-y-6"><div><h1 className="text-3xl font-bold text-slate-900">Audit Log</h1><p className="mt-1 text-slate-500">Jejak aktivitas user untuk kebutuhan pengawasan.</p></div><div className="card overflow-hidden"><div className="overflow-x-auto"><table className="min-w-full text-sm"><thead className="bg-slate-50 text-left text-slate-500"><tr><th className="px-4 py-3">Waktu</th><th className="px-4 py-3">User</th><th className="px-4 py-3">Role</th><th className="px-4 py-3">Action</th><th className="px-4 py-3">Entity</th><th className="px-4 py-3">IP</th></tr></thead><tbody>{rows.map((row) => <tr key={row.id} className="border-t border-slate-100 align-top"><td className="px-4 py-3 whitespace-nowrap">{new Date(row.created_at).toLocaleString('id-ID')}</td><td className="px-4 py-3"><div className="font-medium">{row.full_name || '-'}</div><div className="text-xs text-slate-500">{row.email || '-'}</div></td><td className="px-4 py-3">{row.role || '-'}</td><td className="px-4 py-3">{row.action}</td><td className="px-4 py-3">{row.entity_type} #{row.entity_id || '-'}</td><td className="px-4 py-3">{row.ip_address || '-'}</td></tr>)}</tbody></table></div></div></div>; }
